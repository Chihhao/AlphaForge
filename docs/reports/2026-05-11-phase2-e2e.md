###### tags: `日報`,`AlphaForge`,`Phase 2`,`e2e`

# AlphaForge Phase 2 — notify-hub 整合 e2e 驗收

`文件版本: 2026-05-11a`

## 結果

- **Happy path (sync approval)**: ✅ status=approved, 由 user 在 Telegram 按 [全部同意], `decided_at=2026-05-11T13:44:21Z`, 兩個 item 都 approved
- **Degraded path (hub failure)**: ✅ 用 `NOTIFY_HUB_URL=https://invalid.example.invalid/nh` 強制 ConnectError, helper 自動 fallback 落盤 `docs/proposals/2026-05-11-phase-2-e2e-test.md`, return `status=degraded` + `proposal_path` 絕對路徑
- 全 18 unit tests pass; e2e script 跑 (`backend/scripts/notify_hub_e2e.py`) 雙路徑都對齊預期行為

## NAS notify-hub healthz

```json
{"db":"ok","telegram":"ok","queue_size":0,"version":"0.1.0"}
```

## Happy path stdout

```json
{
  "request_id": "790dad5b-b26c-4f95-a56c-a93ecdab8325",
  "status": "approved",
  "decided_at": "2026-05-11T13:44:21.604566Z",
  "per_item": [
    {"id": "1", "decision": "approved", "reject_reason": null},
    {"id": "2", "decision": "approved", "reject_reason": null}
  ]
}
```

## Degraded path stdout

```json
{
  "status": "degraded",
  "proposal_path": "/Users/chihhaolai/Documents/GitHub/AlphaForge/docs/proposals/2026-05-11-phase-2-e2e-test.md"
}
```

fallback 檔內容含 frontmatter (`status: pending` / `created_at` / `slug` / `reason` / `request_id: null`) + items 各 section + 備援操作說明, 對齊 spec §3.3。

## 過程修正

執行 e2e 時發現 `_fallback_to_proposals()` 原 default `proposals_dir=Path("docs/proposals")` 是 **cwd 相對路徑**, 因 agent 從 `backend/` 跑 → fallback 寫到 `backend/docs/proposals/` 而非 spec §3.3 預期的 repo-root `docs/proposals/`。已修為基於 `__file__` 推導 `repo_root/docs/proposals/`, 18 unit tests 仍全過。

## Commits

- `5059359` feat(agent): notify_hub_client skeleton + ConfigError
- `3896ec8` feat(agent): approve_request POST + httpx MockTransport tests
- `952fda7` feat(agent): wait_result long-poll loop
- `cc420cd` feat(agent): _fallback_to_proposals with slug + collision handling
- `51b5b37` feat(agent): approve_and_wait high-level API + fallback dispatch
- `5c9d542` feat(prompt): night tick Stage 6 — concrete notify_hub_client call
- `fce1b73` feat(agent): backend/.env.example, e2e script, docs/proposals/ dir
- (本 commit): helper default proposals_dir → repo-root anchored + e2e 報告

END: ok
